
## consignment_service.yaml

---
apiVersion: v1
kind: Service
metadata:
  name: consignment
  namespace: kube-system
  labels:
    app: consignment
spec:
  clusterIP: None
  ports:
  - port: 50051
    protocol: TCP
    targetPort: 50051
  selector:
    app: consignment

---


apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: consignment
  namespace: kube-system
spec:
  replicas: 2
  selector:
    matchLabels:
      app: consignment
  template:
    metadata:
      labels:
        app: consignment
    spec:
        containers:
        - name: consignment
          image: gcr.io/gcptraining-17042017/python-grpc-microservice/consignment:nginx
          imagePullPolicy: Always
          ports:
          - containerPort: 50051
            name: cons-port
          resources:
            requests:
              memory: "64Mi"
              cpu: "250m"
            limits:
              memory: "64Mi"
              cpu: "250m"
